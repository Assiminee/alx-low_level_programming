#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int i;
        int j = 10;

        for (i = 97; i <= 122; i++)
        {
                putchar(i);
        }

        putchar(j);

        return (0);
}
